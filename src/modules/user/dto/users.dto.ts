// User DTO placeholder — keep parity with the reference's dto/ folder. Profile
// read endpoints rely on the JWT-derived userId rather than a body, so no
// request DTOs are needed yet. Add request DTOs here as user-management
// endpoints (list, update, delete) come online.
export {};
